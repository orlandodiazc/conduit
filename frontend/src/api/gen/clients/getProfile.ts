/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '../../client.ts'
import type { RequestConfig, ResponseErrorConfig } from '../../client.ts'
import type {
  GetProfileQueryResponse,
  GetProfilePathParams,
  GetProfile400,
  GetProfile404,
} from '../types/GetProfile.ts'

function getGetProfileUrl(username: GetProfilePathParams['username']) {
  return `http://localhost:8080/profiles/${username}` as const
}

/**
 * {@link /profiles/:username}
 */
export async function getProfile(
  username: GetProfilePathParams['username'],
  config: Partial<RequestConfig> & { client?: typeof client } = {},
) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<
    GetProfileQueryResponse,
    ResponseErrorConfig<GetProfile400 | GetProfile404>,
    unknown
  >({
    method: 'GET',
    url: getGetProfileUrl(username).toString(),
    ...requestConfig,
  })
  return res.data
}
